# TPMS 胎壓偵測器資料爬蟲 V10 — 使用說明書

## 程式概述

自動爬取德國 [interpneu 輪胎配置器](https://www.interpneu-raederkonfigurator.de/) 的 TPMS（胎壓監測系統）感測器資料，存入 SQLite 資料庫，並產出各品牌 Excel 與變更紀錄報表。

---

## 流程圖

```
┌─────────────────────────────────────────────────────────┐
│                     程式啟動                               │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  1. 初始化                                             │
│     • 建立輸出目錄 (胎壓檢測器資料庫_V10/)               │
│     • 檢查/升級 DB schema (OE Nummer 欄位)               │
│     • 備份現有 DB → 胎壓檢測器資料庫_V10_備份.db         │
│     • 載入舊資料 (GLOBAL_OLD_DB_DF) 供比對              │
│     • 復原檢查：DB 中有但缺少 Excel 的品牌 → 自動補產    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  2. 取得品牌清單                                        │
│     • API: /api/cars/manufacturers → 91 個 Brand        │
│     • 確認 30 天週期 (超過自動重設進度)                  │
│     • 載入中斷進度 (skip_mode)                           │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  3. 建立 DB table + view                                │
│     • tpms_sensors (含 UNIQUE 約束)                     │
│     • view_tpms_sensors_unique1 (依 Brand/Model/Typ/    │
│       年份 GROUP_CONCAT 合併)                           │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
         ┌───────────────────────────┐
         │  4. 品牌迴圈 (91 個 Brand)  │
         └───────────┬───────────────┘
                     │
         ┌───────────▼───────────────┐
         │   skip_mode 檢查           │
         │                            │
         │  已處理過? ──是──→ 跳過     │
         │                │  確保 Excel 存在 │
         │                └──→ continue    │
         │                            │
         │  中斷點?  ──是──→ 關閉      │
         │               skip_mode    │
         │               繼續從此 Brand │
         └───────────┬───────────────┘
                     │
                     ▼
         ┌───────────────────────────┐
         │  5. Model 迴圈             │
         │     API → get_classes(b)  │
         └───────────┬───────────────┘
                     │
         ┌───────────▼───────────────┐
         │  6. Type-Group 迴圈        │
         │     API → get_type_groups  │
         └───────────┬───────────────┘
                     │
         ┌───────────▼───────────────┐
         │  7. Version 迴圈           │
         │     API → get_versions    │
         │                            │
         │  每個 Version:             │
         │  • get_car_hsn_tsn(tag)   │
         │  • get_tpms(tag)          │
         │  • get_sensor_details(id) │
         │  • 累積 batch_data        │
         │                            │
         │  batch ≥ 80 筆? ──→ 寫入 DB│
         └───────────┬───────────────┘
                     │
                     ▼
         ┌───────────────────────────┐
         │  8. Model 完成             │
         │  • 剩餘 batch → 寫入 DB   │
         │  • 立即寫入 Brand Excel   │
         │  • 計算 diff vs 舊資料    │
         │  • 立即寫入 變更紀錄.xlsx  │
         └───────────┬───────────────┘
                     │
                     ▼
         ┌───────────────────────────┐
         │  9. Brand 完成             │
         │  • 記錄 completed_brands  │
         │  • 儲存進度檔              │
         └───────────┬───────────────┘
                     │
                     ▼
         ┌───────────────────────────┐
         │  10. 下一個 Brand          │
         └───────────────────────────┘
                     │
                     ▼
         ┌───────────────────────────┐
         │  全部完成 或 逾時/中斷      │
         └───────────┬───────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  finally 清理                                           │
│     • 剩餘 batch → 寫入 DB                              │
│     • DB 中缺 Excel 的 Brand → 補產                     │
│     • 最終寫入 變更紀錄.xlsx                              │
│     • SQL 備份 → 胎壓檢測器資料庫_V10.sql                 │
│     • 顯示統計結果                                       │
└─────────────────────────────────────────────────────────┘
```

---

## 目錄結構

```
胎壓檢測器資料庫_V10/
├── AIWAYS_Data.xlsx              ← 各品牌資料 (逐一產出)
├── ALFA ROMEO_Data.xlsx
├── AUDI_Data.xlsx
├── ...
└── 變更紀錄.xlsx                  ← 變更報表 (含多個日期分頁)

程式根目錄:
├── V10.py                        ← 主程式
├── 胎壓檢測器資料庫_V10.db       ← SQLite 資料庫
├── 胎壓檢測器資料庫_V10_備份.db  ← DB 備份 (每次執行覆蓋)
├── 胎壓檢測器資料庫_V10.sql      ← SQL dump 備份
└── scrape_progress_V10.json      ← 進度紀錄 (斷點續傳用)
```

---

## 輸出檔案說明

### 1. 各品牌 Excel (`{Brand}_Data.xlsx`)

| 欄位 | 說明 |
|------|------|
| Brand | 品牌名稱 |
| Model | 型號分類 |
| Typ | 版本名稱 |
| 年份 | 生產年份區間 |
| HSN | 德國 HSN 碼 |
| TSN | 德國 TSN 碼 |
| created date | 感測器年份/批號 |
| OE sensor | OE 感測器編號 |
| OE Nummer | OE 零件號碼 |
| Manufacturer | 感測器製造商 |
| Frequency | 頻率 (433/434/315 MHz) |

### 2. 變更紀錄 (`變更紀錄.xlsx`)

- 每日一個分頁（格式 `YYYYMMDD`）
- 同一天中斷重啟 → 合併到同一頁
- 不同天 → 開新分頁

**顏色標記：**
- 🟢 **新增 (New)** → 整行綠底綠字
- 🟢🟡 **修改 (Updated)** → 舊值 (O) 紅底紅字、新值 (N) 綠底綠字
- ⚪ 無變更 → 顯示「本日無任何變更」

### 3. DB 備份 (`胎壓檢測器資料庫_V10_備份.db`)

- 每次執行開始時自動備份
- 固定檔名，每次覆蓋

### 4. SQL 備份 (`胎壓檢測器資料庫_V10.sql`)

- 程式結束時產出完整 SQL dump
- 可用於還原或匯入其他資料庫

---

## 中斷保護機制

| 情境 | 保護措施 |
|------|---------|
| **正常結束** | 寫入所有資料、Excel、變更紀錄、SQL 備份 |
| **程式崩潰/被 Kill** | `finally` 區塊補產缺漏的 Excel + 寫入變更紀錄 |
| **強制終止 (SIGKILL)** | DB 資料已即時寫入 (每 80 筆 / 每個 Model)；下次啟動時 `recover_missing_excels()` 自動補產 Excel |
| **逾時 (5.8 小時)** | 進度儲存至 JSON，下次自動從斷點繼續 |
| **中斷重啟 (同日)** | 變更紀錄合併到同一日分頁 |
| **中斷重啟 (隔日)** | 變更紀錄開新分頁 |

### 寫入時機（確保即時性）

| 資料 | 寫入時機 |
|------|---------|
| **DB (SQLite)** | • batch 累積 80 筆<br>• 每個 Model 完成<br>• 程式結束 `finally` |
| **Brand Excel** | • 每個 Model 完成 (即時更新)<br>• 啟動時 `recover_missing_excels()`<br>• 結束時 `finally` 補產 |
| **變更紀錄** | • 每個 Model 完成 (即時更新)<br>• 結束時 `finally` 最終寫入 |
| **進度檔 JSON** | • 每個 Brand 開始<br>• 每個 Type-Group |

---

## 異常處理

- **API 請求失敗** → 自動重試 5 次 (backoff)，429 等待 10 秒
- **DB 寫入失敗** → `save_batch_to_sql` 內含 try-except，不影響程式繼續
- **Excel 寫入失敗** → `_write_brand_diff` 內含 try-except
- **無資料** → 感測器資料為空的 version 仍會寫入一筆空值佔位
- **中斷補產異常** → 印出警告，不影響主流程

---

## 執行方式

```bash
# 一般執行 (前景)
python3 V10.py

# 背景執行 (推薦，爬蟲耗時數小時)
nohup python3 -u V10.py > run.log 2>&1 &

# 查看進度
tail -f run.log
```
